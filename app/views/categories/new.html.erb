<h1>New Category</h1>
<%= form_for(@category) do |f| %>

<div class="field">
	<%= f.label :category_id  %><br>
	<%= f.text_field :name  %>
</div>

<div class="actions">
	<%= f.submit %>
</div>

<% end %>